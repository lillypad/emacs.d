sudo: required

services:
  - docker

stages:
  - ubuntu
  - arch
  - windows
  - gentoo

jobs:
  include:
    - stage: ubuntu
      language: emacs-lisp
      script: docker build -f docker/ubuntu/Dockerfile .
    - stage: arch
      language: emacs-lisp
      script: docker build -f docker/arch/Dockerfile .
    - stage: windows
      language: emacs-lisp
      script: docker build -f docker/windows/Dockerfile .
    - stage: gentoo
      language: emacs-lisp
      script: docker build -f docker/gentoo/Dockerfile .
